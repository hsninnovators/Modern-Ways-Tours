const navMenu=document.getElementById('navMenu');document.getElementById('menuBtn')?.addEventListener('click',()=>navMenu?.classList.toggle('open'));document.querySelectorAll('#navMenu a').forEach(a=>a.addEventListener('click',()=>navMenu?.classList.remove('open')));document.querySelectorAll('[data-open]').forEach(btn=>btn.addEventListener('click',()=>document.getElementById(btn.dataset.open)?.classList.add('open')));document.querySelectorAll('[data-close],.modal').forEach(el=>el.addEventListener('click',e=>{if(e.target===el||e.target.hasAttribute('data-close'))el.closest('.modal')?.classList.remove('open')}));const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');io.unobserve(entry.target)}}),{threshold:.14,rootMargin:'0px 0px -40px 0px'});document.querySelectorAll('.reveal-up,.reveal-zoom').forEach(el=>io.observe(el));document.querySelectorAll('.tilt-card').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${y*-7}deg) rotateY(${x*9}deg) translateY(-8px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});let ticking=false;window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(()=>{document.documentElement.style.setProperty('--scrollY',window.scrollY);ticking=false});ticking=true}});
